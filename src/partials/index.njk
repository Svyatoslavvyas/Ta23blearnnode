{%extends 'src/partials/layout.njk'%}
{% block content %}
    <div class="content">
        <h1>It works</h1>
        <h1>hello {{name}}!</h1>
        <ul>
            {% for characters in characters %}
                <li>{{ character.name }}</li>
            {% endfor %}
        </ul>
    </div>
{%endblock%}